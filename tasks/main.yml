---

- include: sshkey.yml
  tags: [sshkey, phase1]

- include: packages.yml
  tags: [linux_packages, phase2, all]

- include: source.yml
  tags: [source, phase2, all]

- include: copy.yml
  tags: [copy, phase2, all, devel]

- include: configure.yml
  tags: [configure, phase2, all, devel]

- include: cron.yml
  when: rcb_rsnapshot_cron == "yes"
  tags: [cron, phase2, all, devel]

- include: keys.yml
  tags: [keys, phase2, all, devel]

- include: test1.yml
  tags: [test1, testall, all, devel]

- include: test2.yml
  tags: [test2, testall, all, devel]

- include: test3.yml
  tags: [test3, testall, all, devel]

- include: test4.yml
  tags: [test4, testall, all, devel]

- include: test5.yml
  tags: [test5, testall, all, devel]

- include: test6.yml
  tags: [test6, testall, all, devel]

# EOF
...
